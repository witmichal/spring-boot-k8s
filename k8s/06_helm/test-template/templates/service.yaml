apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-template.fullname" . }}
  labels:
    {{- include "test-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-template.selectorLabels" . | nindent 4 }}
